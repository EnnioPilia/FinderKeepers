export interface Stats {
  usersCount: number;           // nombre total utilisateurs
  activeUsersCount: number;     // utilisateurs actifs (optionnel)
  adsCount: number;             // nombre total annonces
  lostObjectsCount: number;     // objets perdus
  foundObjectsCount: number;    // objets trouvés

}
